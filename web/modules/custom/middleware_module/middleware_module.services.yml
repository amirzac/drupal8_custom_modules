services:
  middleware_module.middleware.utm:
    class: Drupal\middleware_module\UtmDummyMiddleware
    tags:
      - { name: http_middleware, priority: 450 }

  middleware_module.dummy_subscriber:
    class: Drupal\middleware_module\EventSubscriber\DummySubscriber
    tags:
      - { name: event_subscriber }